# 2026-09-18 IoT-Home 交接：节点 Zigbee 掉线自愈与 I2C 恢复兜底

## 1. 新对话先看这里

1. 先读本入口，再读完整文档：`C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-18.md`。
2. 当前节点已运行干净提交版本 `05fc549`，短地址仍为 `0x2b6a`，信道 26，云端持续收到温度、湿度、光照。
3. 今天晚些时候的主线修复是：节点收到 Zigbee `DEVICE_UNAVAILABLE` / 无活动链路信号后，能自动标记离线、停止无效 APS 补发，并重新 network steering 恢复。
4. 网关仍运行旧固件 `991bcb0-dirty`，本次没有刷写 COM6、没有擦除 Zigbee NVRAM、没有部署服务器、没有 push。
5. 不要 `git add -A`；服务器、其他节点目录、部署脚本和既有未跟踪文件继续保持原样。
6. 热点密码、家庭 Wi‑Fi 密码、MQTT 密码和热点 SSID 均不得写入文档、日志或 Git。
