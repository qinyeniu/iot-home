# 2026-09-22 交接入口（23:12 更新）

1. 先读完整文档：`C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-22.md` 第 19 节；再看 `docs/REMOTE_SWITCH_DESIGN.md` 的 2.e 与 3.1。
2. 当前节点：COM5，BASE MAC `98:88:E0:71:1B:A0`，短地址 `0x82cb`；网关：COM6，BASE MAC `98:88:E0:70:90:DC`。
3. Zigbee：channel `26`，PANID `0xd989`；节点重新上电后 secure rejoin 成功。
4. 根因：两块板近距离摆放时节点默认高功率会使网关接收端饱和；当前节点固件启动时固定设置 `-10 dBm`。这是近距离联调值，节点移远后需重测。
5. `on / off / toggle / toggle` 已验证，状态均正确回报；重复 `off` 为幂等场景，网关已转发，后续遥测持续 `on_off=0`。
6. 已完成约 1 小时稳定性观察：短地址不变、遥测持续、无重复 rejoin；I2C 故障为 0。
7. 1 小时内有 3 次父链路短暂提示，均在约 10–20 ms 内由主动探测恢复，`probe_resteer=0`；判断为可接受短时抖动，但需继续 2–4 小时长观察。
8. 60 分钟统计：rounds=340，attempts=1020，aps_ok=1007，aps_fail=3，unavail=3，recovered=3，queue_fail=0，items_failed=0。
9. 后端复验：**50 passed / 3 skipped**。
10. 固件备份：`backups/firmware/2026-09-22-low-tx-power/`；节点 SHA256 `9090719BCD132060A8364474DAC75346C68DC966E93D5BA0C24F5BDAB5882190`。
11. 网关监视器会话 `13987`，节点监视器会话 `74520`；打开 COM5/COM6 可能触发复位，下一步先检查。
12. 外网 API 仍是 502，本轮未部署；密码/凭据不得写入文档、代码、日志或交接文件。
13. **铁律（每次都执行）：会话/阶段结束必须更新本地交接文档；上下文压缩会丢失记忆，本地化存储才能保留上下文。**
