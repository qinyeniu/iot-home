# ESP-IDF CMakeLists.txt for IoT-Home Gateway
cmake_minimum_required(VERSION 3.16)

# Include the ESP-IDF CMake functions
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Define the project
project(iot-home-gateway)