# common — 共享固件模块

核心设计：**传感器驱动注册表**。

- 每个传感器一个驱动模块（如 `aht20.c`、`bh1750.c`），统一注册接口；
- 新增传感器只加文件 + 注册一行，**不改主逻辑**；
- 统一 I2C 扩展排针：`3V3 / GND / SDA / SCL` + 2 个备用 GPIO，新传感器即插即用；
- MQTT 主题常量集中定义，保持 `iot-home/{网关ID}/...` 规范。
