# 2026-09-22 交接入口（22:41 更新）

1. 先读完整文档：`C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-22.md` 第 17 节；再看 `docs/REMOTE_SWITCH_DESIGN.md` 的 2.e 与 3.1。
2. 当前节点：COM5，BASE MAC `98:88:E0:71:1B:A0`，短地址 `0x82cb`；网关：COM6，BASE MAC `98:88:E0:70:90:DC`。
3. Zigbee：channel `26`，PANID `0xd989`；节点重新上电后 rejoin 成功。
4. 根因：两块板近距离摆放时节点默认高功率造使网关接收端饱和；当前节点固件启动时固定设置 `-10 dBm`。这是近距离联调值，节点移远后需重测。
5. 重新上电后已验证 `on / off / toggle / toggle`，状态均正确并回报；重复 `off` 为幂等场景，网关已转发，后续遥测持续 `on_off=0`。
6. 截至 22:41 已连续观察约 30 分钟：短地址不变、遥测持续；一次父链路短暂提示已由主动探测恢复，未重复 rejoin；APS 失败未继续增长。
7. 后端复验：**50 passed / 3 skipped**。
8. 固件备份：`backups/firmware/2026-09-22-low-tx-power/`；节点 SHA256 `9090719BCD132060A8364474DAC75346C68DC966E93D5BA0C24F5BDAB5882190`。
9. 网关监视器会话 `13987`，节点监视器会话 `74520`；打开 COM5/COM6 可能触发复位，下一步先检查。
10. 外网 API 仍是 502，本轮未部署；密码/凭据不得写入文档、代码、日志或交接文件。
11. **铁律（每次都执行）：会话/阶段结束必须更新本地交接文档；上下文压缩会丢失记忆，本地化存储才能保留上下文。**
