# 2026-09-22 交接入口（09:00 更新）

1. 先读完整文档：C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-22.md（重点第 10、11 节）。
2. **用户已出门，节点被带走、C-to-C 已拔**；节点工作全部挂起，网关仍接 COM6。
3. 远程开关：on/off/toggle + on_off 回传已打通（COM5 旧短地址 0x82cb，设备 gw-001-zb-82cb）；继电器物理确认 + GPIO5 本地按键实测待用户回来。
4. **全链路审计发现丢消息根因**：后端旧版本 QoS0 订阅 + 收包与慢数据库处理耦合；实测网关发 19 帧云端只入库 11。已修复：QoS1 订阅、收发解耦（有界队列+单 worker 保顺序）、批量 INSERT、持久会话（固定 ID iot-home-backend）。本地 broker e2e 两场景零丢失（26 passed/3 skipped）。
5. **网关重启恢复 300s → 40s**（关闭 join-before-WiFi 遗留逻辑），已刷入实测。
6. 网关新增 MQTT 断网补发 outbox（30 条静态队列），已编译刷入；断网实测待配合开关热点。
7. retained 状态不再刷新 last_seen（防陈旧状态误判在线）。
8. **下次第一优先：执行 `python tools/deploy_server.py --confirm` 部署新版本（需用户授权/提供 SSH 密码）**；已可一键完成（同步代码+迁移+重建+验收）。历史 10h 实测送达率 93.3%，报告见 docs/DATA_INTEGRITY_REPORT.md。
9. 已完成首次远程备份：工作分支已推送到 origin/codex/gateway-oled-wifi-checkpoint（含全部提交，已设上游）；服务器仍未部署；未擦 NVRAM；不要 git add -A；密码不落文档。
10. **铁律（每次都执行）：会话结束必须更新本地交接文档；上下文压缩会丢记忆，本地化存储才能保留上下文。**
11. 09:15 已新增：设备能力动态建模（API 返回 capabilities，32 passed）、Grafana 开关状态面板、一键部署脚本；均已推送。
