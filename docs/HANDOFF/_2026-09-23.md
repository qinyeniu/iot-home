# 2026-09-23 交接入口（含夜间开发记录）

1. 先读完整文档：`C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-23.md`；核心设计见 `docs/ATPC_TX_POWER_DESIGN.md`。
2. 当前节点：COM5，短地址 `0x82cb`；网关：COM6。Zigbee channel `26`，PANID `0xd989`。
3. 运行时免烧录功率控制已打通：QUERY、固定手动功率、恢复 AUTO；可用 `tools/rf_power_config.py`。
4. 严格事务边界已完成：网关命令响应用低序号 `1..127`，节点主动 REPORT 用高序号 `0x80..0xFF`；主动上报不能确认本次命令。
5. 只有加密 v2 REPORT 的序号、功率、模式全部匹配且属于用户事务，才会携带本次 `request_id` 发布 confirmed；v1 REPORT 不能证明模式。
6. REPORT 到达时实时判断 90 秒 deadline；超时后迟到精确回包也不能确认旧事务。
7. 120 秒链路陈旧只终止本次用户请求，不删除 NVS 中 durable desired mode/manual power；恢复后自动 QUERY/SET 对账。
8. 幂等固定功率场景已修复：节点本来已是目标状态时也会 confirmed；最终确认前在锁内二次校验，防止 TOCTOU。
9. 最终硬件序列通过：SET 0 → AUTO → SET -10 → AUTO，最终 `auto/-10 dBm`。网关重启后的首次查询可能处于父链路收敛期，等待自动恢复即可。
10. 仅对网关 COM6 执行 `app-flash`；节点本轮未重新烧录；未刷 bootloader/分区表、未擦 Zigbee NVRAM。
11. 固件备份：`backups/firmware/2026-09-23-strict-rf-transaction/`；gateway.bin SHA256 `D1EA34AD6CC0052BBEB95662B38FE79DD029963F21C36284013837D1B646B173`，node.bin SHA256 `8894C2FEA281D663873B054E535B9A6A396F7C8F8439F47D762598E09F41242F`。
12. 后端回归：**50 passed / 3 skipped**；独立复审最终 P0=0、P1=0、P2=0，APPROVE。
13. 安全风险仍待服务器处理：公网 MQTT 疑似允许匿名命令；后续必须关闭匿名并限制主题权限。
14. **铁律（每次都执行）：会话/阶段结束必须更新本地交接文档；上下文压缩会丢失记忆，本地化存储才能可靠保留上下文。**

15. 夜间 03:49 起启动 8 小时整夜长测：`firmware/*/log.overnight-*-20260923-034906.txt`；采集进程仍在后台，早晨需分析并结束。
16. 夜间新功能：RF 功率策略 REST API（GET/POST `/api/devices/{id}/rf-policy`），新增 `app/services/rf_policy.py` 与 18 个测试；真实硬件全序列实测通过，最终 auto/-10。
17. Windows 本地联调关键经验：Python 3.14 需 Selector 循环且 uvicorn 要设 loop="none"，否则 aiomqtt add_reader 失败；见 `tools/_local_hw_test_server.py`。
18. confirmed rf_policy 会落 Metric：`tx_power_dbm`、`rf_mode`(auto=0/manual=1)；API 文档见 `docs/RF_POLICY_API.md`。
19. 当前回归：**68 passed / 3 skipped**；夜间后续：Grafana 功率面板、MQTT 认证/ACL 加固配置（不部署）、早晨长测分析。
20. **铁律：每次必须更新本地交接文档。**
21. Grafana 功率面板已完成（dashboard v2：功率趋势、当前模式、模式记录，id 7/8/9），待部署后查看。

22. MQTT 加固配置已完成但未部署：1883 原生 MQTT、9001 WebSocket、后端/网关账号分离、按网关 ID 限制 ACL，并保留旧共享账号回退。
23. 后端配置优先使用 `MQTT_BACKEND_USER/PASSWORD`，为空时回退 `MQTT_USER/PASSWORD`；详见 `docs/MQTT_SECURITY_HARDENING.md`。
24. 新增整夜日志分析工具 `tools/analyze_overnight_logs.py`，支持输出 JSON/Markdown；已用当前日志试运行。
25. 整夜测试发现节点在启动后约 1266 秒发生 `vPortExitCritical` 断言并重启，随后自动 secure rejoin；根因待继续定位，因此本轮不是完全无中断。
26. 当前验证：后端 `71 passed / 3 skipped`；shell 语法和模拟密码/ACL 生成通过；Docker Desktop 后台未就绪，真实 Mosquitto 容器验证待做。
27. **铁律：每次必须更新本地交接文档。**
28. MQTT 加固已定稿并通过复审：最终态后端/网关账号分离，迁移态可用 `MQTT_ENABLE_LEGACY_ACCOUNT=true` 短期保留旧账号，全程不打开匿名。
29. 最终回归：`73 passed / 3 skipped`；独立代码复审 APPROVE（P0/P1/P2=0）；安全复审确认账号/ACL 设计通过。
30. 明文 1883/9001 仍不得直接暴露公网；部署前必须使用 TLS/WSS、VPN 或防火墙白名单。
31. 新增 `tools/analyze_overnight_logs.py`，可按启动段统计最终/累计运行时长、APS/I2C/队列错误与实际功率变化。
32. Docker Desktop 本机未就绪，真实容器 ACL 实测待做；其他待办见完整交接第 16 节。
33. **铁律：每次必须更新本地交接文档。**
