# 会话交接 2026-09-10（新对话从这里开始看）


## 新对话必读：本地 Git 交接规则（长期有效）

用户要求每一代代码都能通过本地 Git 复现；后续新对话必须遵守：

1. 开场先读取最新 `docs/HANDOFF_YYYY-MM-DD.md`。若用户给出的交接路径不存在，先在 `docs/` 下搜索实际文件，不要凭旧印象继续。
2. 全程中文；每一步先说明要做什么，再实际操作。
3. 开始处理前先查看：`git status --short --branch`、最近提交、未跟踪文件和当前烧录端口，避免把服务器、其他节点或临时诊断代码混在同一个提交里。
4. 每完成一个可复现阶段就做本地提交，包括：正式固件修复、独立诊断工程、关键文档更新。编译通过但未实机验证时，只能写“已构建/已烧录待测”，不能写“完成”。
5. 提交前必须确认真实密码、密钥、`wifi_secrets.h`、`build/`、`sdkconfig`、二进制/日志/`.bak` 不进入 Git。
6. 未经用户明确要求，不要 `git push`；本地提交也要在交接文档记录提交号、验证现象和未完成事项。
7. 临时诊断固件应放在独立目录（如 `firmware/diag_*`），附带 README，说明用途、实机结果和如何恢复正式固件；诊断代码也可以单独提交。
8. 结束对话前更新交接文档：当前板子运行的是哪一版固件、烧在哪个 COM、已验证什么、下一步先做什么。

## 2026-09-11 00:05 最新状态：CN 信道 13 已联网过，新版自愈待路由器恢复复测

- 正式网关 RF/CN 信道检查点：`441c879`（fix: active scan + CN 1-13 + deep-sleep RF recovery）。
- 正式网关看门狗修正检查点：`9bd9ff8`（fix: nearby APs visible 时不再 warm reboot）。
- `441c879` 已构建、烧录 COM6 并实机验证：
  - 初次启动时家庭 2.4G `CU_0D84` 不可见，但健康扫描可见周围 AP；旧 boot watchdog 仍误触发两次 warm reboot。
  - 第二次重启期间 `CU_0D84` 恢复，串口明确显示其工作在 **channel 13**、RSSI 约 -49；CN 1-13 国家码生效后关联成功。
  - 获取 IP：`192.168.10.112`；串口显示 `WiFi OK` 和 `MQTT connected`。
  - 服务器 MQTT 实机收到：
    - `iot-home/gw-001/nodes/gw-001/status` payload `{"ip":"192.168.10.112","uptime":33}`；
    - `iot-home/gw-001/nodes/gw-001/telemetry` payload `{"gw":"online"}`。
- `9bd9ff8` 已构建、烧录 COM6，当前 COM6 运行这一版正式网关（不再是诊断固件）。
- `9bd9ff8` 已实机验证的自愈边界：
  - 启动后连续 3 次完整扫描为 0 时，按设计进入 2 秒 deep sleep；USB 串口短暂消失后 COM6 自动重新枚举，证明定时唤醒链路可工作。
  - 唤醒后健康扫描能看到邻居 AP、但 `CU_0D84` 不可见；日志输出 `Boot watchdog hold ... no reboot`，没有再误热重启。
  - 同时电脑扫描也只能看到 `CU_0D84_5G`，看不到 2.4G `CU_0D84`。
- 尚未完成：当前家庭 2.4G 再次不可见，因此 `9bd9ff8` 还没有完成“从本版本启动到 WiFi OK + MQTT connected + 心跳”的完整复测，也未做 10 分钟稳定性和用户 OLED 目视复核。
- 下一步：保持监听；等 `CU_0D84` 2.4G 恢复后确认 `9bd9ff8` 自动关联 channel 13、拿到 IP、MQTT 心跳恢复，然后再连续观察 10 分钟。若 2.4G 长时间不恢复，需要用户重启/检查路由器 2.4G 射频。网关稳定前不要烧录 COM5。

## 2026-09-10 23:51 最新实机结果：真实断电可恢复板端 RF（优先级最高）

用户已按要求拔掉 COM6 网关 USB 供电，等待 10 秒后重新插入。

实机结果：

- 重新插入后 COM6 枚举正常：USB JTAG/serial debug unit + `USB 串行设备 (COM6)`。
- 因上电后前 60 秒诊断输出已错过，随后通过串口复位刚完成真实上电的板子并抓日志；这不等同于新的拔电，但 RF 状态来自真实断电后的上电。
- `diag_wifi_scan` 立刻恢复扫描能力：8 轮分别稳定看到约 5–6 个周围 2.4GHz AP，包括：
  - `TP-LINK_2558HGM` ch11，RSSI 约 -42～-46；
  - `ZYZ` ch6，RSSI 约 -53～-65；
  - `hayate-wifi` ch8，RSSI 约 -56～-61；
  - `1722` ch6 等。
- 同一时间电脑和板子都看不到 2.4GHz `CU_0D84`，电脑只看到 `CU_0D84_5G`；因此此时不能连接家庭路由器，原因是路由器 2.4GHz 射频又不可见。
- 结论更新：ESP32-C6 不是硬件永久损坏；此前“扫描任何 AP 都为 0”是板端 RF/WiFi 状态卡死，`esp_restart()`/RTS 复位无法恢复，真实 USB 断电 10 秒可以恢复。
- AP 发射测试中电脑未看到 `IOT_RF_TEST`，但当时电脑扫描结果本身异常/不完整，只能作为未决现象，不能据此判定发射链路损坏。
- 下一步代码方向：正式网关增加“周围 AP 连续多次为 0”的 RF 健康检查；若目标路由器不可见但邻居 AP 可见，只继续等待/重连；若所有 2.4GHz AP 连续多次为 0，则用定时器深度睡眠 2 秒后唤醒，模拟这次有效的彻底断电恢复。
- 注意：此时 COM6 仍运行 `diag_wifi_scan`。修改并烧录正式网关前，不要认为 OLED/网关界面处于正式状态。

## 2026-09-10 23:45 最新实机状态：WiFi/RF 诊断中（优先级最高）

- 本地分支：`codex/gateway-oled-wifi-checkpoint`。
- 网关 OLED/WiFi 自愈检查点已本地提交：`c53ab34`（只本地提交，未 push）。
- 纯 WiFi/RF 诊断工程已本地提交：`5c1ec4e`，路径 `firmware/diag_wifi_scan/`。
- **COM6 当前运行的是临时 `diag_wifi_scan` 诊断固件，不是正式网关固件**；此时 OLED 不显示正常网关界面是预期现象，不要据此判断 OLED 回退。
- 已做实机诊断：
  - 电脑曾明确扫描到 `CU_0D84` 2.4GHz：BSSID `68:7d:00:1e:11:84`，信道 3，信号约 91%；MQTT 服务器可连接，但 15 秒内没有网关心跳。
  - 正式网关在 Zigbee 启动前即报 `WiFi disconnected: reason=201/36 rssi=-128`。
  - `diag_wifi_scan` 不启动 OLED/MQTT/Zigbee，8 轮主动扫描周围 AP 数量均为 0。
  - 已擦除 NVS/PHY 区：偏移 `0x9000`、长度 `0x7000`；未擦除应用和 `0x180000` 开始的 Zigbee 分区。重新初始化 PHY 后扫描仍为 0。
  - APSTA 驱动日志显示 `sta + softAP` 已启动，热点名 `IOT_RF_TEST`、信道 6；电脑多次扫描暂未看到该热点。
- 当前判断：问题已收敛到板端 2.4GHz 射频链路、天线/供电、PHY 初始化或极少见的最小驱动兼容问题；已基本排除密码、MQTT、后端、Zigbee 业务代码。但在真实断电复测前，暂不最终定性为硬件损坏。
- 等待用户操作：拔掉 COM6 网关 USB 供电 10 秒后重新插入，再继续抓取诊断日志。
- 真实断电后的下一步：
  1. 先让 `diag_wifi_scan` 再跑至少 2 轮，确认是否仍为 0、电脑是否能看到 `IOT_RF_TEST`；
  2. 无论结果如何，恢复正式网关：进入 `firmware/gateway` 后执行 `idf.py -p COM6 app-flash`（诊断只写了应用区；如启动异常再完整 `flash`）；
  3. 恢复后用 OLED + 串口 + MQTT 心跳共同确认 WiFi/MQTT；
  4. 网关未稳定前不要烧录 COM5 终端。
- 本文件后续的文档提交号通过 `git log --oneline -5` 查看；交接时以最新提交和本节为准。


## 一句话状态


## 2026-09-10 深夜续测（最新，优先级高于上文）
- OLED：本地 SSD1306 5x7 文本驱动已由用户目视确认正常；官方 `espressif/ssd1306` 1.0.5 文字方案已从依赖移除。
- 网关 WiFi 重复 NO 的实机判据：`reason=201 rssi=-128`（NO_AP_FOUND）和 `reason=36`（ASSOC_EXPIRE），不是密码/MQTT 问题。
- 已做固件修复并编译/烧录 COM6：
  - `WIFI_ALL_CHANNEL_SCAN` 全信道扫描；
  - `WIFI_STORAGE_RAM`，不沿用 NVS 旧 BSSID/信道缓存；
  - 断线永久打印 reason/rssi；
  - 启动看门狗计数从 NVS 改为 RTC 魔数计数，软件热重启最多 2 次，重新断电自动重新获得 2 次机会，拿 IP 后清零。
- 实机结果：2.4G 短暂恢复时，第 1 次热重启后拿到 `192.168.10.112` 且 `MQTT connected`；2.4G 再次不可见时，1/2、2/2 热重启后仍是 201/36。仍需重启/修复路由器 2.4G 后做 10 分钟稳定观察。
- WiFi 明文凭据已移出源码：真实文件是本机 `firmware/gateway/main/wifi_secrets.h`（git-ignored），模板是 `wifi_secrets.h.example`。
- 新详细排查文档：`docs/GATEWAY_WIFI_RECOVERY.md`。
- Git：外层仓库此前把 `firmware/gateway` 记录成无 `.gitmodules` 的失效 gitlink，已在本地修复为普通目录并提交检查点；本次只本地 commit，不 push。
- 终端 COM5 仍未烧录；Zigbee→网关→MQTT→后端→Grafana 端到端联调仍未完成。

## 2026-09-10 晚间续测（实机结果，优先级高于上文旧记录）
- OLED：用户目视确认**正常**（文字可读、方向正确、黄蓝分区正确、稳定不闪）。
- 重要修正：`espressif/ssd1306` 1.0.5 在本机实机显示为乱码/诡异字形，不能按上文“官方组件最终定稿”继续。
  已在 `firmware/gateway/main/main.c` 改为本地 SSD1306 文本驱动：标准 128x64 初始化（含 `0xDA 0x12`）、
  5x7 ASCII 字库、page-major 帧缓冲、刷屏时转换为 vertical addressing，并一次连续写入 1024 字节。
- 改动前故障版备份：`firmware/gateway/main/main.c.20260910-oled-debug.bak`。
- WiFi：修复 OLED 后实机仍显示 NO。90 秒串口实测一直未关联到路由器、无 IP，MQTT 因此 Host unreachable；
  Zigbee 协调器可独立启动。电脑两次扫描均看不到 2.4G `CU_0D84`，只看到 5G `CU_0D84_5G`，
  当前首要排查路由器 2.4G 射频是否关闭/改名/隐藏，或重启路由器。
- 曾临时加入板载 WiFi scan 诊断，但该诊断版出现重启循环；代码已完全撤回并烧回正式 OLED 固件，
  后续不要直接复用那段临时扫描实现。
- 终端 COM5 尚未烧录；Zigbee→网关→MQTT→后端→Grafana 端到端联调仍未完成，需先恢复网关 WiFi/MQTT。

Zigbee 数据传输功能代码已完成、两端均编译通过；网关侧稳定性/WiFi/OLED 三大问题今天已修复并烧录实机验证（WiFi+MQTT+Zigbee 都正常）；
**唯一待确认：OLED 换官方驱动后的目视效果；待做：烧录终端节点并端到端联调。**

## 当前实机
- 网关 ESP32-C6（带 OLED，COM6）：已烧最新固件，家里 WiFi 下 WiFi OK / MQTT connected / Zigbee 协调器正常
- 终端 ESP32-C6（AHT20+BH1750，COM5）：**尚未烧 v3.0 ZCL 固件**（只编译过）
- 服务器：API http://8.163.110.27:8000  Grafana :3000  MQTT 8.163.110.27:1883（broker 允许匿名）
- 家里 WiFi：SSID `CU_0D84`（2.4G；密码只保存在本机 `firmware/gateway/main/wifi_secrets.h`，不要写入仓库；历史记录曾在信道 13/7 间变化）
- 手机热点 `qyn`（密码不入库；2.4G ch6）：板子能连 WiFi 但 TCP 到服务器被限（MQTT 连不上，电脑同热点可连），**不要用它联调**

## 本次功能：Zigbee 标准 ZCL 数据传输（设计文档 docs/ZIGBEE数据传输设计.md）
- 终端 node_zigbee/main/main.c v3.0：EP10 注册 Basic/Identify + 温度0x0402/湿度0x0405/照度0x0400 测量 server cluster；
  每 10s 向协调器 0x0000 单播 3 条 Report Attributes（温湿度 0.01 单位；照度 Zigbee 对数刻度 10000*log10(lux+1)）
- 网关：EP10 注册 client cluster + esp_zb_core_action_handler_register 统一回调；
  按源短地址缓存、2s 聚合成一条 MQTT
- 主题 `iot-home/gw-001/nodes/zb-{4位hex}/telemetry`
  payload 必须是 `{"data":{"temperature":x,"humidity":x,"lux":x}}`（后端 mqtt.py 只遍历 data 内数值入库）
  设备自动注册 ID 形如 gw-001-zb-0e8a
- 设备加入通知改发 nodes/zb-{addr}/status，payload {"status":"online",...}
- 注意 esp-zigbee-lib 1.6.0 真实命名（已踩坑）：
  esp_zb_zcl_cluster_list_create()；湿度 cluster/attr 带 REL_；照度 attr 是 ..._MEASURED_VALUE_ID；
  统一回调用 esp_zb_core_action_handler_register（没有单独的 report 注册函数）

## 本次修复的网关问题（均已实机验证）
1. 栈溢出崩溃：Zigbee ZDO 信号跑在系统事件任务，栈 2304→4096
   （sdkconfig.defaults 和 sdkconfig 都设了 CONFIG_ESP_SYSTEM_EVENT_TASK_STACK_SIZE=4096）
2. WiFi 断线回调里 vTaskDelay 会堵 Zigbee 信号 → 改为独立 wifi-reconn 任务（栈 5120）做指数退避重连
3. **冷启动 WiFi 必失败、热复位（按 RST）必成功** → 加启动看门狗：15s（代码宏 WIFI_BOOT_GRACE_MS，实际烧的版本是 25000）
   内没拿到 IP 就 esp_restart()，NVS(namespace "wifi_boot", key "rst") 计数最多 2 次，连上后清零
4. WIFI_PS_NONE（省电关闭，配合某些 AP）
5. OLED：弃用自写分页驱动，改用**官方组件 espressif/ssd1306 1.0.5**（垂直寻址+单次 1024B 事务，100kHz，
   create 后补发 0xC8 修正本屏方向）。文档 docs/hardware/OLED技术文档.md 已写死规范
   ⚠️ 用户还没目视确认；若上下颠倒去掉 oled_cmd_raw(0xC8)，左右镜像改 A1→A0

## 下一步（按顺序）
1. 用户目视确认 OLED：不闪/不撕裂/方向对/黄标题蓝内容（UI：IOT-HOME GW / IP / WIFI:OK MQTT:OK / UP / ZB NODES:n）
2. 网关换充电宝供电（冷启动约 35s 内自动重启一次后联网，看 OLED 确认 OK）
3. 终端板插电脑（COM5）：cd firmware/node_zigbee; idf.py -p COM5 flash monitor
4. 端到端验收：网关日志见 ZCL report + MQTT -> nodes/zb-xxxx/telemetry；
   后端日志"遥测数据已保存: gw-001-zb-xxxx"；Grafana/API 有温湿度光照曲线；稳定观察 10 分钟
5. 之后：深度睡眠低功耗（docs/ZIGBEE_LOW_POWER_DESIGN.md）、多终端、命令下发

## 构建环境
- ESP-IDF v5.5.4：. C:\Espressif\v5.5.4\esp-idf\export.ps1 后 idf.py build/flash
- 读串口辅助：python tools/_read_com.py（pyserial，会软复位 COM6 抓过滤日志）
  MQTT 连通性测试：python tools/_mqtt_probe.py

## 未提交
改动未 git commit；主要改动：firmware/gateway/main/main.c、firmware/node_zigbee/main/main.c、
firmware/gateway/main/idf_component.yml、firmware/gateway/sdkconfig(.defaults)、
docs/PROGRESS.md、docs/ZIGBEE数据传输设计.md、docs/hardware/OLED技术文档.md、本文档


