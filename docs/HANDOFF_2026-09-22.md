# 2026-09-22 IoT-Home 交接：传感器板集成远程开关，On/Off/Toggle 与状态回传已打通

## 1. 新对话先看这里

1. 用户明确：**先不做第三块独立板**，把远程开关集成到原来的传感器节点上。
2. 当前设备仍是同一块原传感器节点：COM5，BASE MAC `98:88:E0:71:1B:A0`，短地址 `0x82cb`，云端设备 `gw-001-zb-82cb`。
3. 端到端远程控制已实测：`on / off / toggle` 均能从云端 -> MQTT -> 网关 -> Zigbee -> 节点执行。
4. 新增的开关状态回传也已实测：节点用 ZCL Report Attributes 上报 OnOff，网关转 MQTT；云端 metrics 中已有指标 `on_off`，值为 0/1。状态上报有独立的发送确认和最多 3 次有限重试。
5. 测试结束后已发送 off，最终状态 `on_off=0`。
6. 物理继电器是否已经接到 GPIO4、是否听到咔哒声，仍需用户确认；软件链路和 GPIO 日志已经验证。
7. 未 push、未部署服务器、未整片擦 Flash、未擦 Zigbee NVRAM、未刷 bootloader/分区表。
8. 继续遵守：不读取/打印密码文件；不要 `git add -A`。
9. **铁律：每次会话/每个阶段结束都必须更新本地交接文档（用户明确要求，每一次都执行）。上下文压缩必然丢失记忆，只有本地化存储能可靠保留上下文。已固化到根目录 `AGENTS.md` 和 `docs/HANDOFF/README.md`。**

## 2. 当前硬件与链路

| 角色 | 端口 | BASE MAC | 当前角色 | 备注 |
|---|---:|---|---|---|
| 网关 | COM6 | 98:88:E0:70:90:DC | Zigbee coordinator + Wi‑Fi/MQTT bridge | OLED 已拔，不影响链路 |
| 二合一节点 | COM5 | 98:88:E0:71:1B:A0 | AHT20/BH1750 传感器 + On/Off 继电器 | 短地址 0x82cb，rx-on-when-idle |

链路：

```text
REST API -> MySQL commands -> MQTT cmd topic
  -> 网关解析短地址与 command
  -> Zigbee ZCL On/Off cluster (0x0006) 单播
  -> 节点 EP10 On/Off server
  -> GPIO4 控制继电器

节点 OnOff state -> 原始 ZCL Report Attributes
  -> 网关缓存/转换 -> MQTT telemetry
  -> 后端 metrics 表（metric = on_off）
```

本次重启后网关日志显示当前 PANID 为 `0xd989`（channel 26）。节点仍以 `0x82cb` 正常加入，不影响云端设备 ID。后续不要因为 PANID 与早期实验室 PAN `0x2b9e` 不同而主动改网；只有重新 clean formation 时才使用代码里保留的固定 PAN 逻辑。

## 3. 代码改动

### 节点：`firmware/node_zigbee/main/main.c`

- 启动时配置 GPIO4 为输出并默认拉高；继电器模块为 3.3V **低电平触发**：LOW=吸合，HIGH=释放。
- 在原 EP10 的手工簇列表中继续加入三个测量 server cluster，并新增 On/Off server cluster（`ESP_ZB_ZCL_CLUSTER_ID_ON_OFF`，0x0006）。
- 注册/复用 `esp_zb_core_action_handler_register(zb_action_handler)`：
  - HA 库把标准 On/Off 命令转成 `ESP_ZB_CORE_SET_ATTR_VALUE_CB_ID`；
  - 校验 cluster=0x0006、attribute=0、size=1；
  - 调用 `relay_set()` 改变 GPIO4；
  - 100 ms 后发送一次 OnOff 状态 report。
- OnOff report 没有使用高层 `esp_zb_zcl_report_attr_cmd_req()`，而是沿用本工程传感器已验证的 raw APSDE-DATA 路径，构造 Boolean 类型（0x10）的 Report Attributes 帧。
- 代码审查后又增加可靠性处理：
  - 用独立的 relay report pending state 记录在途帧 sequence/attempts；
  - APS confirm 严格按 ASDU 长度与 Boolean 类型区分传感器帧（8 字节）和继电器帧（7 字节）；
  - 链路探测窗口占用 sequence 时，500 ms 后重排状态上报；本地提交失败或 APS 失败时最多尝试 3 次；
  - GPIO4 在 `app_main` 最开始配置并保持高电平，同时启用内部上拉；实际高安全负载仍建议加外部上拉。

### 网关：`firmware/gateway/main/main.c`

- 订阅 `iot-home/+/nodes/+/cmd`。
- `gateway_handle_node_cmd()` 从 topic 提取 `zb-XXXX`，解析 payload 中的 `command`：
  - `on` -> `ESP_ZB_ZCL_CMD_ON_OFF_ON_ID`（0x01）
  - `off` -> `ESP_ZB_ZCL_CMD_ON_OFF_OFF_ID`（0x00）
  - `toggle` -> `ESP_ZB_ZCL_CMD_ON_OFF_TOGGLE_ID`（0x02）
- 使用 `esp_zb_zcl_on_off_cmd_req()` 单播到节点，短地址/endpoint 均匹配。
- 网关 EP10 添加 On/Off client cluster。
- ZCL report callback 增加 cluster 0x0006：读取 1 字节 Boolean 值并转成 telemetry：
  - 立即发布 `{"data":{"on_off":0|1}}`
  - 后续完整传感器包发布 `{"data":{"temperature":...,"humidity":...,"lux":...,"on_off":0|1}}`
- main 组件已链接 cJSON（前序提交已完成）。

## 4. 可靠性修补前初测记录（2026-09-22 00:06-00:12）

本节记录第一次打通链路时的结果；当时网关日志字段仍叫 `zcl_status`，后续已更名为 `zcl_seq`。

命令记录：

| commands.id | command | API 返回 | 实际结果 |
|---:|---|---|---|
| 13 | on | sent | 节点执行，但第一次高层状态上报触发断言，节点重启 |
| 14 | on | sent | raw report 成功，网关收到 cluster=0x0006 value=1 |
| 15 | off | sent | 网关收到 on_off=0 |
| 16 | toggle | sent | 从 0 切到 1，网关收到 on_off=1 |
| 17 | off | sent | 最终恢复 on_off=0 |

以下为可靠性修补前的网关日志（仅保留历史现场）：

```text
CMD fwd: node=0x82cb cmd=on zcl_status=1
MQTT -> nodes/zb-82cb/telemetry: {"data":{"on_off":1}}
Zigbee: cached report node=0x82cb cluster=0x0006 value=1 total=97
MQTT -> nodes/zb-82cb/telemetry: {"data":{"temperature":31.2,"humidity":73.3,"lux":17.5,"on_off":1}}

CMD fwd: node=0x82cb cmd=off zcl_status=4
MQTT -> nodes/zb-82cb/telemetry: {"data":{"on_off":0}}
```

云端查询也确认 `metric=on_off` 已入库，最新值为 0：

```text
GET /api/devices/gw-001-zb-82cb/metrics?metric=on_off&hours=1&limit=5
```

## 5. 可靠性增强后复测（00:24-00:25）

代码审查发现“继电器 APS confirm 可能与传感器 confirm 混淆”和“链路切换时状态 report 可能丢失”，已完成修补：

| commands.id | command | 节点/网关结果 |
|---:|---|---|
| 18 | on | `RELAY state report sent: ON attempt=1/3`，随后 `RELAY state report acknowledged attempt=1/3`；网关收到 `{"on_off":1}` |
| 19 | off | 云端最新 `metric=on_off` 已恢复为 0 |

复测期间温度、湿度、光照继续每 10 秒上报；完整 telemetry 中继续包含 `on_off`。至此继电器状态帧不会再误占传感器上报窗口；在弱网或链路切换时也会做有限重试。后续周期性 telemetry 仍会携带当前 `on_off`，构成状态补偿。

## 6. 踩坑：高层 OnOff report 断言

最初节点在 `relay_state_report_timer_cb()` 中使用：

```text
esp_zb_zcl_report_attr_cmd_req()
```

节点已经打印 `RELAY ON`，随后 ZBOSS 断言：

```text
Assertion failed .../zcl/zcl_general_commands.c:612
abort() was called
```

因此停止使用该高层 helper，改为 raw APSDE-DATA 自己构造 ZCL Report Attributes：

- frame control：server-to-client report，0x18
- command：Report Attributes，0x0a
- cluster：0x0006
- attribute：0x0000
- data type：Boolean，0x10
- value：1 字节，0 或 1

改用 raw report 后，on/off/toggle 均不再触发断言，传感器每 10 秒上报继续正常。

## 7. 仍待确认/后续任务

1. 请用户确认继电器实际接线：IN -> GPIO4、VCC -> 3V3、GND；确认是否听到咔哒声，或用万用表/小灯泡验证输出端通断。
2. 设备类型仍注册为 `sensor`。由于它现在是“传感器 + 开关”组合设备，后续可：
   - 扩展设备类型/能力字段；或
   - 在设备详情中根据 `on_off` 指标识别 switch capability。
   不建议简单改成纯 `switch`，否则会弱化传感器属性。
3. commands 表状态目前仍是 `sent`，尚未在执行后改为 `done/acknowledged`。
4. 本地按键手动控制继电器尚未实现。
5. Grafana 可增加一个开关状态/开关按钮面板；目前 `on_off` 已作为普通指标入库。

## 8. 工作区杂项提醒

- `firmware/node_zigbee/460800/` 是误建构建目录，仍未清理。
- `firmware/node_switch_zigbee/` 保留为“第三块独立板”的备选方案，当前不刷、不作为主线。
- 旧 `firmware/node_switch/`、`firmware/node_combined/` 是 9/6 Wi‑Fi 直连旧方案，勿用。
- 多个未跟踪 tools 调试脚本和 `cmd_*.json` 仍在；提交时只加明确文件，不能 `git add -A`。

## 9. 00:27 追加：固化“每次必须写交接文档”铁律

用户明确要求（原话要点）：

- agent 经过上下文压缩后总会丢掉一些记忆；
- 通过把上下文写入本地文件来解决这个问题；
- **以后每一次会话都必须执行，不能忘**。

本次已落地到三个位置（互为保险）：

1. `AGENTS.md`（项目根目录，新会话自动读取）：写明铁律与交接流程；
2. `docs/HANDOFF/README.md`：交接目录规则、每次流程、全部入口索引；
3. 本文件与 `docs/HANDOFF/_2026-09-22.md`：记录本条要求本身。

以后收工标准动作：更新当天完整交接 + 入口摘要 → 显式 git add 这些路径 → 提交。


## 10. 08:10 追加：后端闭环已提交 + 全链路稳定性复查（进行中）

### 本次会话已完成（均已提交，未 push）

1. `8cea9d1 feat(backend)`：开关命令闭环 ack/timeout/supersede（复审 P0/P1=0）。
2. `4c61884 harden(backend)`：metrics 查询参数边界（hours 1–8760、limit 1–5000）、on_off 拒绝 JSON 布尔、MySQL 测试清理参数化；新增 `tests/test_api_validation.py`（6 项）。全套 26 passed / 1 skipped。
3. `db5220b feat(node)`：GPIO5 本地按键（低电平有效、300ms 消抖、20ms 轮询），按一次切换继电器并走与云端命令完全相同的 relay_set + raw ZCL report 路径。已 app-flash 到 COM5，编译通过（ESP-IDF v5.5.4，C:\Espressif 自包含环境，激活脚本 `C:\Espressif\tools\Microsoft.v5.5.4.PowerShell_profile.ps1`；注意 C:\esp\v5.5.5 那套与现有 build 目录不匹配，用它需 fullclean）。

### Git 历史梳理结论

- 全仓库 104 提交：master 含 8 月硬件/采购、MicroPython 验证、9/3 终端验收、9/5 docker-compose；当前分支在此之上多 73 个提交（9/6 Zigbee 转型至今）。
- **远程 origin 只有初始提交，本地领先 103 个提交，从未备份到服务器**。push 需用户明确授权。
- 节点按键 2 分钟测试窗口内未捕获按键动作（日志无 RELAY 切换），按键可能尚未接 GPIO5，待用户接线后复验。

### 稳定性复查发现的异常（调查中，本节持续更新）

- 云端 07:08–08:07 一小时 temperature 样本 322 条（按 10s 周期应 ~360），大量实际间隔 20–40s，并有 1–3.5 分钟缺口（如 07:46:49→07:50:16）。
- 但节点本地 REPORT_STATS：attempts=57 / aps_ok=56 / aps_fail=0，即节点侧帧几乎全部被网关 APS 确认。
- 同一 MQTT 帧的指标 created_at 相差可达 6 秒（08:06:44 的湿度 47 秒入库、光照 53 秒入库）。
- 初步指向：网关 MQTT 发布 / Wi-Fi(热点) / 低配 VPS MySQL 写入慢导致消息处理积压丢失；线上后端仍是旧版本（今天的闭环代码未部署，metrics/latest 返回中无 received_at 字段可证）。
- 下一步：抓 COM6 网关日志，与云端时间对齐，确认丢帧发生在哪一段。

## 11. 09:00 追加：全链路稳定性加固审计结论 + 本轮全部修复（用户出门期间完成）

### 硬件状态变化

- 用户约 08:24 出门，**拔掉了节点的 C-to-C 线并把节点带走**；节点相关工作（按键验证、继电器物理确认、rejoin 实测）全部挂起，待用户回来继续。
- 网关 COM6 仍接着电脑和热点，本轮网关工作均已完成并刷写验证。

### 审计方法与证据链

按"节点 → 网关 → broker → 后端 → MySQL"逐段对齐时间戳：

| 段 | 结论 | 证据 |
|---|---|---|
| 节点 → 网关 | 健康 | 节点 REPORT_STATS：attempts=57 / aps_ok=56 / aps_fail=0；i2c_* 全部 0 |
| 网关内解析/发布 | 健康 | 08:10-08:14 共 200s：收到 19 个完整三属性帧，全部发布 MQTT，无断连/无去重误杀 |
| **broker → 后端 → MySQL（旧版本）** | **丢消息** | 同窗口云端只入库 **11/19**（约 42% 丢失）；同帧指标 created_at 相差最大 6s |
| 网关重启恢复（旧版本） | **白等 3-5 分钟** | 启动先等节点 join：commissioning 180s / 已知 child rejoin 300s，节点不在时必然等满 |

### 根因

1. 后端以**默认 QoS0 订阅**遥测/状态，且"收包 → 解析 → 逐条指标 INSERT"全在单个网络协程内串行；低配 VPS 上数据库一旦抖动，MQTT 收包循环被阻塞，QoS0 消息在 broker/网络层静默丢失（无任何重传）。
2. `join-before-WiFi` 是 9/6 射频共存实验期的临时方案；共存问题（de86d94、3c166c2、799c5d0 等）解决后，这个等待只剩副作用：节点不在时每次重启云端 3-5 分钟不可见。
3. 附带：后端重连时 broker 投递的 **retained 状态**会被旧代码当成新心跳刷新 last_seen，掉线设备可能被误判在线（最长一个离线超时窗口）。
4. 附带：后端默认随机 client ID + clean session，短暂重启期间 broker 不保留消息。

### 已实施修复（均已提交、未 push；后端 26 passed / 3 skipped）

| 提交 | 内容 |
|---|---|
| `1488c6f` | 后端 QoS1 订阅；网络循环只把消息投入有界队列（2000），独立单 worker 串行处理保持同设备顺序；每帧指标批量 INSERT（一次事务）；本地 broker e2e：慢处理（150ms/帧）60 帧×3 指标零丢失；broker 断连重启 30 帧零丢失 |
| `0f10636` | 后端固定 client ID `iot-home-backend` + MQTT 3.1.1 + 持久会话（clean_session=False），后端短暂重启时 broker 排队补发 |
| `958a81d` | 网关关闭 join-before-WiFi，Wi-Fi/MQTT 与 Zigbee 并行恢复；retained 状态不再刷新 last_seen；**实测重启恢复 300s → 40s**（08:50:22 重置，08:51:02 last_seen 更新） |
| 网关 outbox（与 958a81d 同批代码） | 新增 MQTT 断网补发队列：静态内存 30 条（≈5 分钟），断连期间 telemetry 入队、重连后最旧优先补发，队列满丢最旧并告警；retained 状态不入队（重连后 status_resync 自动重发）。已编译并随固件刷入；**断网实测待用户回来配合**（开关热点制造窗口） |

本地 e2e 复跑命令：

```powershell
$env:IOT_RUN_LOCAL_BROKER = "1"
.\.venv\Scripts\python.exe -m pytest tests/test_e2e_local_broker.py -q
```

### 历史稳定性加固代码盘点（完整性核对）

- 节点：上报失败重试 6c3ff61、数值校验 aae7f6c、AHT20 CRC d175857、现代 I2C 驱动 d553a2a、I2C 重建 804eb4f、断链恢复系列（05fc549 → 1e20ba3 共 9 个提交）——代码在位；稳态有 7.8h+ 运行证据；rejoin 主动场景待节点回来复验。
- 网关：共存 RF 优先级/窗口系列（7629bf8、f42e03b、1b1d20b、de86d94、3c166c2）、PAN 保留 ebb7c72、专用栈循环 799c5d0、OLED 恢复 0231198——代码在位且网关当前运行版本包含全部。
- 服务器：MySQL 启动等待 7a862b1、多段设备 ID 解析 fa41573、离线判定 fbda99b、OOM 修复 ad3354b、Grafana lux a9793c4、MQTT 认证 daaf0f9、凭据环境化 5ab7057——代码在位。

### 仍待办（下次继续，按优先级）

1. **部署后端新版本到服务器**（需要用户明确授权；不部署则线上仍按旧行为丢消息）：更新 /root/iot-home 代码 → 执行 migration `2026-09-22_command_lifecycle.sql`（或确认 init 已是最新）→ 重建后端容器；部署后用"网关帧数 vs 云端入库数"对齐做实测验收。
2. 用户回来后：接回节点 → 确认节点 rejoin/短地址（可能再次变化，属正常）→ 验证按键（GPIO5）与继电器咔哒声。
3. 网关 outbox 断网补发实测（开关热点 1-2 分钟窗口，看 `MQTT outbox flushed` 日志）。
4. 设备能力建模（传感器+开关组合）、Grafana 开关按钮面板仍未做。
5. 所有代码仍未 push（本地领先 origin 103+ 提交）；是否备份到远程等用户决定。

### 边界遵守情况

- 未 push、未部署、未擦 Flash/NVRAM、未刷 bootloader/分区表（节点/网关均只 app-flash）；未 git add -A；密码文件未读取/落文档。
- 多个 tools/_*.py 临时补丁脚本未跟踪、未提交，保持原样。

## 12. 09:05 追加：已完成首次远程备份（用户明确授权）

- 推送工作分支到 GitHub：`origin/codex/gateway-oled-wifi-checkpoint`（d9ec1f9，与本地 0/0 差异，含全部 100+ 提交），已设置上游跟踪。
- `origin/master` 与本地 master 一致（0cdd681，此前已同步）。
- 仓库：https://github.com/qinyeniu/iot-home
- 注意：GitHub 默认分支 `main` 仍是无关的旧初始提交（8c58405）；如需让仓库首页直接显示最新代码，可由用户在 GitHub 设置把默认分支改为 `codex/gateway-oled-wifi-checkpoint`（或之后授权我合并/更新 main，本次未强推 main）。
- PR 入口：https://github.com/qinyeniu/iot-home/pull/new/codex/gateway-oled-wifi-checkpoint

## 13. 09:15 追加：用户外出远控期间持续完成的工作（均已提交推送）

1. `57e2bbc feat(backend)` 设备能力建模：新增 `app/services/capabilities.py`，从设备最近 7 天上报的指标动态推导能力 `{"sensors": [...], "switch": bool}`；设备列表（批量一次查询）和详情均返回 `capabilities`；无需改表/迁移。新增 6 项测试，全套 32 passed / 3 skipped。组合设备保持 type=sensor，开关通过 capabilities.switch 表达。
2. `718dc52 feat(grafana)` 开关面板：看板新增"开关当前状态"stat 卡片（0 已关闭/红、1 已开启/绿）和"开关状态记录"阶梯曲线；随仓库提供，部署时重启 grafana 即生效。
3. `834bc0c` 数据完整性报告：`docs/DATA_INTEGRITY_REPORT.md`，工具 `tools/analyze_integrity.py`（未跟踪）。9h57m 窗口：应到 3361、实到 3135，**送达率 93.3%（永久丢失 6.7%）**；558 次单帧缺口；确认旧行为 = 迟到入库（保留原始 ts）+ 永久丢失（08:10-08:14 窗口 19 帧：当时 11 → 后补到 14 → 仍缺 5）。
4. `834bc0c` 一键部署脚本：`tools/deploy_server.py`。`python tools/deploy_server.py` 离线看计划（无需连接）；`--confirm` 执行：git 同步分支 → stdin 方式向 mysql 容器执行迁移 → up -d --build backend → restart grafana → 只读验收。认证用 SSH key 或 IOT_HOME_SSH_PASSWORD；不改动服务器 .env。
5. 线上后端仍是旧版本，丢帧持续；节点不在无法做完整部署验收——用户回来授权 + 接回节点后执行最佳。


## 14. 15:55 追加：修复 QoS1“已确认但未落库”，补齐 message_id 全链路

### 14.1 复查最后推送版本（903c1d1）发现的问题

1. **网关没有生成 `message_id`**：此前只在后端准备了按 `message_id` 去重的表/逻辑入口，但真实网关遥测仍只有 `{"data": ...}`，后端无法判断两帧是否为同一帧重投。
2. **旧版 paho 在调用应用回调后立即自动 PUBACK**：消息刚进入异步队列、数据库事务尚未提交时，broker 就已收到确认。若后端在此时崩溃，broker 不会重投，仍可能永久丢失。
3. **关停窗口不一致**：后端设计了 20 秒 drain，但 lifespan 只等待 5 秒，Docker 默认宽限期也可能提前 SIGKILL，导致“关停时已收消息写完”不完整。

### 14.2 本次修复

1. **网关统一生成并注入 `message_id`**：
   - 首次非 retained 遥测时在 NVS 命名空间 `gwmqtt` 中维护持久 `boot_epoch`，每次启动递增；
   - ID 组成：Wi‑Fi MAC + boot epoch + tick ms + 帧序号，例如 `<mac>-b00000001-t00001234-s00001`；
   - 在 `mqtt_pub()` 中央注入，所有非 retained 遥测自动携带；
   - MQTT outbox 保存的是注入后的完整 payload，断网补发时同一帧不会变号；
   - retained status 不纳入这套遥测 ID。
2. **后端升级为手动确认**：
   - `aiomqtt 2.5.1` + `paho-mqtt 2.1.0`；
   - 只有遥测事务提交后才手动 PUBACK；
   - 永久坏消息记录日志后确认丢弃；
   - 数据库错误不确认，broker 可重投；重试用尽且 dead letter 落盘成功后确认；
   - 每帧记录底层 MQTT 连接，禁止用新连接确认旧连接的 packet id。
3. **幂等表保留与清理**：
   - 新表 `processed_messages(message_id PK, device_id, processed_at)`；
   - 初始化 SQL 与迁移 `2026-09-22_command_lifecycle.sql` 均已包含；
   - 保留 30 天，后台每 6 小时清理一次，避免无限增长。
4. **关停窗口对齐**：
   - lifespan 等待 `SHUTDOWN_DRAIN_TIMEOUT + 5 = 25s`；
   - `docker-compose-minimal.yml` / `docker-compose.yml` backend 设置 `stop_grace_period: 25s`。
5. **部署脚本验收增强**：`tools/deploy_server.py` 除 `metrics.received_at` 外，还会验收 `processed_messages` 表存在。

### 14.3 本地验证

- 新增测试：`server/backend/tests/test_message_dedup.py`，覆盖重复 ID 只入库一次、on_off 重复确认、跨设备 ID 冲突、非法 ID、旧固件兼容、保留期清理、以及“事务成功后才确认”。
- 后端全套：**50 passed / 3 skipped / 6 subtests passed**。
- 网关固件已编译，并通过 `idf.py -p COM6 app-flash` **只刷应用分区**；未刷 bootloader/分区表，未擦 Flash/NVRAM。
- 监视启动日志确认：
  - 网关连接电脑热点 `DESKTOP-O6O2BC0 8856`，IP `192.168.137.76`；
  - `MQTT connected`；
  - Zigbee coordinator 启动，PANID `0xd989`，channel `26`；
  - 因无节点，open network 180 秒后自动关闭，属正常。

### 14.4 当前外部状态与下一步

1. 云服务器 API 当前从外网访问返回 **502 Bad Gateway**（80/8000 都一样）；本次未部署、未改服务器。需要用户明确授权后才能登录服务器诊断，或直接执行新版部署。
2. 节点仍被用户带出门，节点 rejoin、继电器 GPIO4/按键 GPIO5、节点侧全链路复测继续挂起。
3. 用户回来并授权后，建议执行：`python tools/deploy_server.py --confirm`，部署后使用网关帧数与云端入库数再次对齐验收。
4. 本次结束仍已更新交接文件；上下文压缩会丢失记忆，**必须继续通过本地文档保留上下文**。


## 15. 19:52 追加：用户授权部署，但当前缺少可用 SSH 凭据

1. 用户明确回复“授权”，已获得登录/部署服务器许可。
2. 执行 `python tools/deploy_server.py --confirm` 时，服务器可达，但 Paramiko 返回：`No authentication methods available`。
3. 本机 `C:/Users/HJB/.ssh` 只有 `known_hosts`，没有私钥；`ssh -o BatchMode=yes root@8.163.110.27` 也确认无密钥可用。
4. 已尝试系统凭据弹窗与自动启动可见终端；凭据弹窗/窗口在远控环境中未确认可见。部署未开始，服务器尚未被本次操作修改。
5. 已新增手动部署入口：
   - `C:/Users/HJB/Documents/iot-home/tools/部署服务器.bat`
   - `C:/Users/HJB/Documents/iot-home/tools/interactive_deploy.ps1`
   - 日志：`C:/Users/HJB/Documents/iot-home/tools/deploy_server_20260922.log`
6. 用户下一步可双击 bat，在可见窗口输入 `root` 密码；脚本只把密码放在当前进程环境变量中，部署结束即清除，不写入日志/文档。
7. 若用户更愿意直接提供密码，也只能用于本次部署；不要把密码写入交接文档或仓库。
8. 完成部署后需继续追加：部署 SHA、迁移结果、health 结果、processed_messages 验收结果、外网 502 是否解除。

## 16. 21:04 追加：节点已接回但仍无法入网；已排除后端、MQTT 与 Wi‑Fi 共存

### 16.1 本轮恢复动作

1. 已按项目铁律先读 `docs/HANDOFF/README.md`、最新入口和当天完整交接，再开始工作。
2. 用户已把原传感器节点接回：`COM5`，BASE MAC `98:88:E0:71:1B:A0`。
3. 用户提供了一次服务器密码；该密码未写入文档、代码、日志或交接文件。
4. 清理了上一轮遗留的重复 `COM6` 监视进程，避免串口占用和重复复位。

### 16.2 节点与网关实测

1. 普通监听 `COM5` 时，节点持续输出：
   - `Rejoin failure: no dev with xpanid 98:88:e0:ff:fe:70:90:dc or can't choose best parent`
   - `Startup/rejoin failed (0xffffffff), bdb status=3; retry steering`
2. 重新启动网关后确认：
   - 热点连接成功，IP `192.168.137.103`；
   - MQTT connected；
   - Zigbee coordinator 启动成功；
   - Channel `26`，PANID `0xd989`；
   - permit join 开放 180 秒。
3. 使用开发环境监视器对节点执行可靠复位后，节点仍重复上述失败。
4. 网关侧在节点复位和多次 steering 期间没有出现 child join、rejoin 请求或其他相关 Zigbee 日志。

### 16.3 无线共存隔离诊断

1. 临时把网关 `ZB_ONLY_RF_DIAG` 从 0 改为 1，使网关只启动 Zigbee，不启动 Wi‑Fi/MQTT。
2. 通过 `esptool write_flash 0x10000` **只刷应用分区**，没有擦 Flash、没有擦 Zigbee NVRAM、没有刷 bootloader/分区表。
3. 诊断固件确认 coordinator 仍在 channel `26` / PANID `0xd989`，并开放 join。
4. 节点可靠复位后仍完全扫描不到网关，失败报文相同。
5. 结论：当前故障不是后端入库、MQTT、热点或 Wi‑Fi/802.15.4 共存导致；优先怀疑物理 RF/天线/接线，或其中一端 Zigbee 状态/射频链路异常。
6. 诊断结束后已把 `ZB_ONLY_RF_DIAG` 恢复为 0，重新编译并只刷应用分区。正常网关已再次验证：热点 IP `192.168.137.103`、MQTT connected、coordinator channel `26` / PANID `0xd989`。
7. 源代码中的临时诊断开关已恢复，`firmware/gateway/main/main.c` 无最终代码差异。

### 16.4 稳定性与云端复验

1. 独立复跑后端测试：**50 passed / 3 skipped**。
2. 外网 `http://8.163.110.27/api/health` 当前仍返回 **502 Bad Gateway**。
3. 使用用户提供的密码进行 SSH 认证时再次返回 `Authentication failed`；服务器未登录成功，部署未开始，服务器未被本次操作修改。

### 16.5 下一步

1. P0：请用户检查两块板的天线方向、是否靠近金属、节点/网关接线是否松动；建议两块板相距约 1–2 米，避免紧贴造成接收饱和。
2. P0：在关闭串口监视器后，对网关和节点分别手动重新上电；网关启动并开放 join 后，再复位节点。
3. 若仍失败，需要用户明确授权后才能考虑清理/重置 Zigbee 状态；当前不得主动擦 Zigbee NVRAM。
4. P1：请用户确认 root 密码是否有大小写或字符变化，或提供 SSH key；凭据可用后再执行 `python tools/deploy_server.py --confirm`。
5. P2：节点入网后复测 `on / off / toggle`、`on_off` 回传、继电器物理声、GPIO5 本地按键。
6. 每次会话和每个阶段结束仍必须更新本地交接文档；上下文压缩会丢记忆，本地化文档是保留项目上下文的可靠方式。

## 17. 22:35 追加：-10 dBm 修复后重新上电复验通过

### 17.1 根因与修复

1. 用户重新上电前，节点无法 rejoin 的根因已通过独立 802.15.4 诊断定位：两块板同桌面近距离摆放时，节点默认高功率发射导致网关接收端饱和/阻塞。
2. 节点固件已在 `firmware/node_zigbee/main/main.c` 中加入：
   - `#define ZIGBEE_NODE_TX_POWER_DBM (-10)`；
   - `esp_zb_init(&zb_cfg);` 后调用 `esp_zb_set_tx_power(ZIGBEE_NODE_TX_POWER_DBM)`；
   - 随后调用 `esp_zb_get_tx_power()` 并打印实际值。
3. 节点启动日志确认：`Zigbee TX power=-10 dBm`。
4. 修复后节点 secure rejoin 一次成功，短地址保持/恢复为 `0x82cb`；channel `26`、PANID `0xd989` 未改变。
5. `-10 dBm` 是当前近距离联调配置；节点移远后必须重新评估，不得视为永久生产参数。

### 17.2 重新上电后的全链路实测

用户重新上电后，在不额外打开串口、不抢占既有监视器的前提下完成 MQTT -> 网关 -> Zigbee -> 节点验证：

| 时间 | 命令 | 节点日志 | MQTT/状态结果 |
|---|---|---|---|
| 22:23:46 | on | `RELAY ON`，report 一次确认 | 立即收到 `on_off=1` |
| 22:23:58 | off | `RELAY OFF`，report 一次确认 | 立即收到 `on_off=0` |
| 22:24:10 | toggle | `RELAY ON`，report 一次确认 | 立即收到 `on_off=1` |
| 22:24:22 | toggle | `RELAY OFF`，report 一次确认 | 立即收到 `on_off=0` |
| 22:24:34 | off（重复幂等） | 状态未变化，无新 `RELAY OFF` | 网关已 `CMD fwd ... zcl_seq=9`，后续遥测持续为 0 |

结论：状态变化命令全部执行并回报；重复 off 为标准幂等场景，不要求产生新的状态变化帧。验证结束时继电器为 off。

### 17.3 稳定性观察

1. 截至 22:32，节点本次上电后连续运行约 20 分钟，短地址始终为 `0x82cb`。
2. 温度、湿度、光照持续采样，网关持续转发 MQTT。
3. 节点在 uptime 约 732 秒时出现一次父链路短暂不可用提示：
   - 主动探测帧 `seq=212`；
   - 约 20 ms 内提示 resolved；
   - 未触发 steering/rejoin，未更换短地址。
4. 节点最近一组统计（rounds=98）：
   - `i2c_th_fail=0`、`i2c_lux_fail=0`、`i2c_resets=0`、`i2c_reinits=0`；
   - `aps_fail=1`、`unavail=1`、`recovered=1`；
   - `probe_resteer=0`，未见重复入网。
5. 网关侧未发现影响控制链路的 MQTT 断连；偶发 Wi‑Fi block-ack 日志未中断遥测。

### 17.4 代码审查

独立代码审查结论：**APPROVE，CRITICAL/HIGH/MEDIUM/LOW 均为 0**。确认事项：

- `esp_zb_set_tx_power()` 在 `esp_zb_init()` 之后、`esp_zb_start(false)` 之前调用，时机正确；
- ESP Zigbee 1.6.0 中该 API 返回 `void`，因此当前通过回读和启动日志确认配置是合适做法；
- TX power 与 `rx-on-when-idle`、Zigbee NVRAM、PANID/channel/短地址均无冲突；
- 建议后续真实部署前把功率改为可配置并按现场链路重新标定。

### 17.5 后端复验

使用项目已有虚拟环境 `server/backend/.venv/Scripts/python.exe` 复跑：

```text
50 passed, 3 skipped
```

### 17.6 固件备份

最终正式应用固件已备份到：

```text
backups/firmware/2026-09-22-low-tx-power/
```

文件与哈希：

| 文件 | SHA256 |
|---|---|
| iot-home-zigbee-node-low-tx-power.bin | 9090719BCD132060A8364474DAC75346C68DC966E93D5BA0C24F5BDAB5882190 |
| iot-home-gateway.bin | AD3B2A569079901F12026B1E71C6CE504298C0C13D1F01199FBD0C20E7FA5F6D |

这些备份是应用固件；恢复时只写应用分区 `0x10000`，不得擦 Zigbee NVRAM、bootloader 或分区表，除非用户再次明确授权。

### 17.7 当前仍可能运行的会话

- 网关监视器：会话 `13987`，日志 `firmware/gateway/log.gateway-verify-20260922220930.txt`。
- 节点监视器：会话 `74520`，日志 `firmware/node_zigbee/log.node-verify-20260922221022.txt`。
- 打开 COM5/COM6 仍可能触发复位；下次先检查这些会话。

### 17.8 下一步

1. 继续做 1 小时以上稳定性观察，确认无重复 rejoin、无 APS failure 增长、遥测持续。
2. 请用户确认物理继电器是否听到咔哒声，或用万用表/小灯验证输出端。
3. GPIO5 本地按键代码已在固件中，但仍需实际接线后测试。
4. 外网 API 仍为 502；服务器部署问题未在本轮处理。恢复部署前继续遵守用户授权和凭据不落盘原则。
5. 每次会话/阶段结束必须更新本地交接文档；上下文压缩会丢失记忆，本地化文档是可靠保留上下文的方式。

## 18. 22:41 追加：30 分钟稳定性观察通过

1. 节点本次上电后 uptime 约 `1799059 ms`（约 30 分钟），短地址始终为 `0x82cb`，未重复 steering/rejoin。
2. 最新节点统计（rounds=170）：
   - `attempts=510`、`queued=510`、`queue_fail=0`、`items_failed=0`；
   - `aps_ok=503`、`aps_fail=1`，相比 20 分钟观察点没有新增 APS 失败；
   - `unavail=1`、`recovered=1`、`probe_resteer=0`；
   - `i2c_th_fail=0`、`i2c_lux_fail=0`、`i2c_resets=0`、`i2c_reinits=0`。
3. 网关 uptime 约 `1875454 ms`（约 31 分钟），持续缓存三属性并发布 MQTT；最新继电器状态仍为 `on_off=0`。
4. 网关在约 23:44/30:19（相对 uptime）出现过两次 `NLME status indication nwk_status=0x09 network_addr=0x82cb`，代码已将这类周期性 NWK/地址验证状态视为健康网络可接受事件；未伴随断连、重复 rejoin 或遥测中断。
5. 启动早期出现一次 `WiFi set scan parameters failed: ESP_ERR_INVALID_STATE`，原因是扫描参数设置时 STA 已在扫描；后续热点/MQTT 正常，未再影响链路。
6. 结论：`-10 dBm` 修复后的 Zigbee 节点在当前近距离桌面布局下，30 分钟全链路稳定性观察通过。下一阶段可继续延长到 1 小时，或等待用户确认继电器物理声音/接线后测试 GPIO5 按键。

## 19. 23:12 追加：1 小时稳定性观察通过（含 3 次短时自动恢复）

### 19.1 观察窗口

- 节点从本次重新上电/secure rejoin 后持续运行，到达 `3604279 ms`（约 60.1 分钟），短地址始终为 `0x82cb`。
- 到 23:11 最新节点 uptime 约 `3668239 ms`（61.1 分钟），温湿度/光照仍在持续上报。
- 网关 uptime 约 `3720714 ms`（62.0 分钟），MQTT 保持连接，持续发布遥测。

### 19.2 1 小时节点统计

在约 60 分钟统计点（rounds=340）：

| 项目 | 数值 | 解释 |
|---|---:|---|
| attempts / queued | 1020 / 1020 | 340 轮采样，每轮 3 个测量属性，均进入发送队列 |
| queue_fail / items_failed | 0 / 0 | 无队列丢弃、无最终失败项 |
| aps_ok / aps_fail | 1007 / 3 | 绝大多数 APS 成功；3 次失败与 3 次短暂父链路提示对应 |
| unavail / recovered | 3 / 3 | 3 次父链路短暂提示均由主动探测恢复 |
| probes / probe_aps_fail | 3/3 / 0 | 主动探测均成功 |
| probe_resteer | 0 | 没有触发重新 steering/rejoin |
| i2c_th_fail / i2c_lux_fail | 0 / 0 | 温湿度与光照 I2C 采样无失败 |
| i2c_resets / i2c_reinits | 0 / 0 | 没有 I2C 复位/重建 |

到 61 分钟最新统计（rounds=346）：`attempts=1038`、`aps_ok=1027`、`aps_fail=3`、`unavail=3`、`recovered=3`，说明最近几分钟没有新增异常。

### 19.3 三次短时提示时间

| 事件 | 节点 uptime | 恢复证据 |
|---|---:|---|
| event=1 | 732199 ms | seq=212，约 20 ms resolved |
| event=2 | 2013639 ms | seq=68，约 10 ms resolved |
| event=3 | 2100759 ms | seq=96，约 10 ms resolved |

判断：这是可自动恢复的短时父链路抖动，不是掉线；没有短地址变化，没有重复 rejoin，也没有造成遥测长时间中断。由于 1 小时内出现 3 次，后续仍建议继续做更长观察，并保持 `-10 dBm` 近距离联调设置，不要在未实测情况下继续随意降低或提高功率。

### 19.4 网关侧观察

1. 网关持续缓存三属性并发布完整 telemetry，继电器最终持续为 `on_off=0`。
2. 网关多次出现 `NLME status indication nwk_status=0x09 network_addr=0x82cb`，代码已将这类周期性网络/地址验证状态视为健康网络可接受事件；未伴随 MQTT 断开或重复 rejoin。
3. Wi‑Fi block-ack 曾出现删除/重建，未影响 MQTT connected 和遥测发布。
4. 启动早期一次扫描参数设置失败已被后续正常连接证明无持续影响。

### 19.5 结论

`-10 dBm` 修复后的“节点 -> 网关 -> MQTT”链路，在当前近距离桌面布局下完成 1 小时稳定性观察：**通过，但存在 3 次已自动恢复的短暂父链路提示**。下一步优先继续 2–4 小时长观察；硬件人工确认仍包括继电器物理咔哒声和 GPIO5 本地按键。
