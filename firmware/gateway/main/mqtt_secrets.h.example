#pragma once

// Copy this file to mqtt_secrets.h and enter the MQTT account from server/.env.
// mqtt_secrets.h is ignored and must never be committed.
#define MQTT_USERNAME           "iot_user"
#define MQTT_PASSWORD           "your-mqtt-password"
