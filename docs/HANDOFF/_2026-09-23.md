# 2026-09-23 交接入口（02:18 更新）

1. 先读完整文档：`C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-23.md`；核心设计见 `docs/ATPC_TX_POWER_DESIGN.md`。
2. 当前节点：COM5，BASE MAC `98:88:E0:71:1B:A0`，短地址 `0x82cb`；网关：COM6，BASE MAC `98:88:E0:70:90:DC`。
3. Zigbee：channel `26`，PANID `0xd989`；节点复位后加入成功。
4. ATPC 已修正：节点每次固定 **-10 dBm** 安全启动；只有真实入网/APS 失败达到阈值才提升；LQI 或延迟读取 RSSI 不再单独触发提升。
5. 网关 join/announce 后会清空旧状态并 QUERY；收到加密 REPORT 才更新功率。私有 REPORT 丢失时 90 秒后重新 QUERY。
6. 近距离约 5 分钟实测：网关一直保持 -10 dBm；节点 rounds=25、attempts=75、aps_fail=0、unavail=0、I2C failures=0，MQTT 遥测持续。
7. 实测日志：
   - `firmware/node_zigbee/log.atpc-failure-driven-node-20260923011224.txt`
   - `firmware/gateway/log.atpc-failure-driven-gw-20260923011144.txt`
8. 固件备份：`backups/firmware/2026-09-23-failure-driven-atpc/`；节点 SHA256 `4CE8CE21672A80EAE8D3BCEB36958A0520DEEB31E6752E7D4F647A7DA978FD94`。
9. 后端回归复验：**50 passed / 3 skipped**。
10. 未 push、未部署、未擦 NVRAM/Flash、未刷 bootloader/分区表；密码不得落盘。
11. ATPC 自动功率范围已做成安装配置：默认启动 `-10 dBm`，自动范围 `-10..+20 dBm`；节点改 `ZIGBEE_NODE_TX_POWER_AUTO_MIN_DBM/MAX_DBM`，网关改 `RF_POWER_AUTO_MIN_DBM/MAX_DBM`。
12. 范围改动后节点、网关均编译通过；表外启动值/上下限会编译失败，避免静默就近映射；固件备份在 `backups/firmware/2026-09-23-configurable-atpc-bounds/`。
13. 自动边界配置已重新烧录并完成硬件实测；默认值逻辑与当前策略一致，近距离 5 分钟和 3 分钟复验均通过。
14. 复审结论：P1 已关闭，未发现新的 P0/P1；自动边界配置可进入烧录复验/长测阶段。
15. 已烧录复验：节点 COM5、网关 COM6；网关保持运行、仅复位节点的 3 分钟测试中 `aps_fail=0`、`queue_fail=0`、I2C failures=0；一次 parent unavailable 立即探针恢复，网关持续保持 -10 dBm。
16. 同冷启动观察：节点/网关同启时曾有约 106 秒父链路验证不一致，随后自动 resteer 恢复；这是已知边界，后续优化启动顺序/收敛时间。
17. 新复验日志：
   - `firmware/node_zigbee/log.atpc-bounds-nodeonly-202609230206.txt`
   - `firmware/gateway/log.atpc-bounds-gw-nodeonly-202609230206.txt`
18. 新增并加固 `tools/capture_serial_no_reset.py`：不主动脉冲 RTS/DTR，串口/文件资源异常安全，日志实时落盘；复审通过。注意打开 USB-Serial/JTAG 端口本身仍可能复位。
19. **铁律（每次都执行）：会话/阶段结束必须更新本地交接文档；上下文压缩会丢失记忆，本地化存储才能可靠保留上下文。**
