# 2026-09-18 交接入口

完整交接文档：

`C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-18.md`

新对话先读本入口，再读完整交接文档。节点固件已在提交 `804eb4f fix(node): rebuild I2C bus after repeated failures` 中加入 I2C 总线/设备句柄重建兜底，并已只刷 COM5 应用分区实测；当前节点运行干净版本 `804eb4f`，短地址仍为 `0x2b6a`，云端持续收到温度、湿度、光照。刷写后曾遇到约 145 秒找不到 Zigbee 父节点；只读接入 COM6 监视器时网关串口自动复位，随后节点恢复入网，网关和云端链路正常。没有烧录 COM6、没有部署服务器、没有擦除 Zigbee NVRAM、没有 push。热点密码、家庭 Wi‑Fi 密码和 MQTT 密码均不得写入文档、日志或 Git。
