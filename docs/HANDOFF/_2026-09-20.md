# 2026-09-20 交接入口

1. 新对话先读本入口，再读完整文档：`C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-20.md`。
2. 网关 COM6 已运行干净固件 `9296fe9`，BASE MAC 为 `98:88:E0:70:90:DC`；2026-09-20 08:47 后云端恢复 online。
3. 节点补丁已提交为 `e734c8b fix(node): verify parent availability before resteering`，已编译为干净版本，但**尚未刷入 COM5**；节点现网仍是 `05fc549` 的旧行为。
4. 08:42 安全重启网关后，节点曾短暂 rejoin 到 `0x2b6a`，但旧固件收到 signal 60 后立即 resteering，随后持续 `no dev with xpanid ... can't choose best parent`；节点仍 offline，完整遥测停在 08:30:52。
5. 新节点补丁会在 signal 60 后发送独立单属性 APS probe，只有精确探测帧 APS 成功才判定恢复；5 秒无成功才 resteer。最终代码审查为 APPROVE。
6. 刷 COM5 前必须再次得到用户明确授权，先核对 BASE MAC `98:88:E0:71:1B:A0`，然后只执行 `idf.py -p COM5 --baud 460800 app-flash`。
7. 禁止整片擦除、刷 bootloader/分区表、擦 `zb_storage` / `zb_fct`、改 PAN/channel/XPANID 或任何凭据。
8. 不要 push，不要部署服务器，不要 `git add -A`；服务器、其他节点目录、部署脚本和既有未跟踪文件保持原样。
9. 热点 SSID、热点密码、家庭 Wi‑Fi 密码、MQTT 密码均不得写入文档、日志或 Git。
