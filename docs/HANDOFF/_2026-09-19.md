# 2026-09-19 IoT-Home 交接入口：网关 Zigbee 主循环与诊断补丁

1. 新对话先读本入口，再读完整文档：`C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-19.md`。
2. 网关源码补丁已提交：`799c5d0 fix(gateway): use dedicated Zigbee stack loop`。
3. 该版本已在本机编译通过，应用版本为干净的 `799c5d0`，但**尚未刷入 COM6 网关**；网关当前仍应运行旧固件 `991bcb0-dirty`，刷写前必须看启动日志确认。
4. 本次没有改变 PAN、信道、端点、Zigbee NVRAM、Wi‑Fi、MQTT 凭据或服务器逻辑；没有擦除 `zb_storage` / `zb_fct`。
5. 节点 COM5 仍运行昨天的干净版本 `05fc549`，短地址 `0x2b6a`；21:45 云端网关、节点均在线，温湿度/光照持续更新。
6. 下次硬件方便且有人看顾时，才可核对 COM6 BASE MAC 为 `98:88:E0:70:90:DC` 后只执行 `app-flash`；禁止整片擦除、刷 bootloader/分区表或擦 Zigbee NVRAM。
7. 不要 `git add -A`，不要 push，不要部署服务器；服务器、其他节点目录、部署脚本和既有未跟踪文件继续保持原样。
8. 热点 SSID、热点密码、家庭 Wi‑Fi 密码、MQTT 密码均不得写入文档、日志或 Git。
