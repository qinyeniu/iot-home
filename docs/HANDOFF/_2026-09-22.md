# 2026-09-22 交接入口

1. 先读完整文档：C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-22.md。
2. 用户已确认先不做第三块板，开关集成在原传感器节点（COM5，短地址 0x82cb）。
3. on/off/toggle 已端到端实测；GPIO4 控制继电器，低电平吸合、高电平释放。
4. OnOff 状态回传已打通并有独立确认/最多 3 次重试：节点 ZCL report -> 网关 -> MQTT -> metrics 表，指标名 on_off，最终值为 0。
5. 踩坑：esp_zb_zcl_report_attr_cmd_req() 会触发 zcl_general_commands.c:612 断言；已改为 raw APSDE-DATA Boolean report。
6. 仍需用户确认物理继电器接线/咔哒声。
7. 未 push、未部署、未擦 NVRAM；不要 git add -A；密码不落文档。
8. **铁律（用户要求，每次都执行）：会话结束必须更新本地交接文档；上下文压缩会丢记忆，本地化存储才能保留上下文。已写入 `AGENTS.md` 和 `docs/HANDOFF/README.md`。**
