> **2026-09-22 00:12 更新：远程开关与 `on_off` 状态回传已完成，最新上下文请读 `docs/HANDOFF_2026-09-22.md`。本文保留 00:02 前的链路恢复记录。**
# 2026-09-21 IoT-Home 交接：链路已恢复，网关/节点运行 1e20ba3，节点新短地址 0x82cb

## 1. 新对话先看这里

1. 链路已恢复，双端均 online 并持续入库（00:01 确认）。
2. 当前固件：网关 COM6、节点 COM5 都运行干净构建的 **1e20ba3**（ESP-IDF v5.5.4）。
3. **节点短地址变了**：从历史的 0x2b6a 变为 **0x82cb**，云端对应设备 `gw-001-zb-82cb`。
   旧设备 `gw-001-zb-2b6a` 保留为 offline，历史数据仍在。这是今天网络实验后的正常结果，已接受。
4. 本次断链根因很简单：**节点被移动到了远处**（用户确认），恢复原位后链路立刻正常；硬件没有坏。
5. 未 push、未部署服务器、未整片擦除、未刷 bootloader/分区表。
6. 不要 `git add -A`；密码不落文档。
7. 夜间调查细节见 `docs/HANDOFF_2026-09-20-NIGHT.md`；白天背景见 `docs/HANDOFF_2026-09-20.md`。

## 2. 当前设备与网络

| 角色 | 端口 | BASE MAC | 固件 | 状态 |
|---|---:|---|---|---|
| 节点 | COM5 | 98:88:E0:71:1B:A0 | 1e20ba3（iot-home-zigbee-node） | online，短地址 0x82cb |
| 网关 | COM6 | 98:88:E0:70:90:DC | 1e20ba3（iot-home-gateway） | online，协调器 |

链路：节点 -> Zigbee -> 网关 -> Wi‑Fi -> MQTT -> FastAPI -> MySQL。

云端设备：

- 网关：`gw-001-gw-001`
- 节点（当前）：`gw-001-zb-82cb`
- 节点（历史，offline）：`gw-001-zb-2b6a`

## 3. Git 状态

分支：`codex/gateway-oled-wifi-checkpoint`。

今晚把丢失对话工作区里的 MM 半成品（"先回退再演进"）理清后，落成两个干净提交：

```text
1e20ba3 fix(node): verify provisional rejoins with sparse active probes
ebb7c72 fix(gateway): preserve lab PAN ID on fresh network formation
2b4eb22 (今天 16:44，丢失对话的最后提交)
```

两个工程都在提交后干净重新构建，版本号均显示 1e20ba3。

- `iot-home-gateway.bin`：0x154260 字节，应用分区 8% 空闲。
- `iot-home-zigbee-node.bin`：0xd44e0 字节，应用分区 42% 空闲。

### ebb7c72（网关）
干净 ZBOSS NVRAM 重新组网时，显式请求实验室 PAN 0x2b9e（channel 26），
不再让协议栈随机选 PAN；扩展身份/密钥仍由协议栈重新生成。新增 `#include "nwk/esp_zigbee_nwk.h"`。

### 1e20ba3（节点）
provisional rejoin 的验证方式从"普通传感器批次被动验证"改为稀疏单属性主动探测：
先 settle 2 秒，30 秒窗口内最多 10 次、每次间隔 2.5 秒；探测期间独占射频窗口、
阻断普通三属性批次，消除 rejoin 后的失败 APS 风暴。本地提交被拒不再消耗重试次数；
首次采样前用温度标准无效值 0x8000 探测，网关会丢弃该值（实测确认 `raw==0x8000 -> return ESP_OK`）。

## 4. 恢复刷写记录（20:40 备份 → 写回 → 刷应用）

诊断固件每次启动都擦 Zigbee NVRAM，恢复时顺序为：先写回网络数据，再刷应用固件。

1. 用 `C:\Users\HJB\.iot-home\role-swap-backup-20260920-204015\` 中的
   `*-0x180000-0x4000.bin`（zb_storage）与 `*-0x184000-0x1000.bin`（zb_fct）
   分别写回对应板子（COM6 网关、COM5 节点）。
2. 再 `idf.py app-flash` 刷入 1e20ba3 应用（只刷 factory app 分区）。
3. 启动后节点未以 0x2b6a secure rejoin，而是重新关联获得 0x82cb。
   原因：20:40 的网关备份里已无 0x2b6a 子设备记录（白天实验中该子设备老化被删；
   19:47 的网关备份才有，但两份网关 NVRAM 差异大、无法确认密钥一致，不冒险混用）。

## 5. 最终验收

- 23:58 节点以 0x82cb 关联成功并开始上报；网关、节点 23:59 均 online。
- 00:01 复查：双端仍 online；最新样本 temperature=31.7、humidity=63.3、lux=18.3，时间戳连续。
- 23:48 的角色互换 220 秒测试全程稳定无断开，证明射频链路正常。

云端只读检查：

```text
curl.exe --noproxy "*" -s http://8.163.110.27:8000/api/devices/gw-001-gw-001
curl.exe --noproxy "*" -s http://8.163.110.27:8000/api/devices/gw-001-zb-82cb
curl.exe --noproxy "*" -s http://8.163.110.27:8000/api/metrics/latest
```

## 6. Grafana 说明

看板用设备下拉变量 `$device_id`（`SELECT device_id FROM metrics GROUP BY device_id ...`），
不绑定固定 ID。新设备 `gw-001-zb-82cb` 会自动出现在下拉框，选择它即可看当前曲线；
选 `gw-001-zb-2b6a` 可看今天 16:03 前的历史。无需改看板。

## 7. 硬件备注

- 网关 OLED 显示屏已被用户拔掉（I2C SSD1306）。当前云端链路不受影响。
  若需要屏幕显示，插回即可；下次看串口时可留意 OLED I2C 是否刷初始化失败日志。
- 节点对距离非常敏感（远处 LQI 低到 5–15，无法关联；并排时正常）。
  以后移动节点后若不在线，先确认摆位距离，再考虑其他原因。

## 8. 待清理杂项（未执行，保持原样）

- `firmware/node_zigbee/460800/`：误建的构建目录（波特率被当成 -B 目录），未跟踪垃圾；本机策略拦截了脚本删除，待用户确认后手动删。
- 今晚在 `tools/` 新建的小工具（capture_serial.py、inspect_zb_backups.py、cmp_gw_nvram.py）及 node_net_diff.txt，未跟踪，可留作调试或清理。
- 角色互换诊断工程与全部抓包日志在 `C:\Users\HJB\.iot-home\`（仓库外），其中有多份 zb NVRAM 备份。

## 9. 严格边界（继续遵守）

- 不读、不打印 wifi_secrets.h、mqtt_secrets.h；密码不落文档/日志/Git。
- 不 push，不部署服务器；不整片擦 Flash、不刷 bootloader/分区表、不随意擦 zb NVRAM。
- 不用 `git add -A`，不把未跟踪文件混入提交。
- 打开 COM5/COM6 本身可能触发板子复位；设备稳定后优先查云端。
