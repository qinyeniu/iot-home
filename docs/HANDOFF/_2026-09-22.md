# 2026-09-22 交接入口（15:55 更新）

1. 先读完整文档：C:/Users/HJB/Documents/iot-home/docs/HANDOFF_2026-09-22.md（重点第 14 节）。
2. 用户已出门，原传感器节点和 C-to-C 线被带走；节点工作挂起，网关仍接 COM6，并已连电脑热点。
3. 15:55 已修复最后推送版本的关键隐患：旧版 paho 在数据库提交前自动 PUBACK，崩溃仍会丢消息；网关也未生成 message_id。现已升级为手动确认、网关注入持久 message_id、后端 processed_messages 幂等去重。
4. 网关 message_id：NVS 命名空间 `gwmqtt` 维护 boot_epoch，ID = MAC + boot_epoch + tick + seq；outbox 补发时保持同一 ID。
5. processed_messages 保留 30 天，每 6 小时自动清理；部署脚本会验收该表。
6. 关停 drain 窗口已统一为 25 秒，FastAPI 与 Docker stop_grace_period 已对齐。
7. 验证结果：后端全套 **50 passed / 3 skipped / 6 subtests passed**；网关已 app-flash，只刷应用分区，未擦 NVRAM/Flash。
8. 网关启动实测：连接热点 IP 192.168.137.76，MQTT connected，Zigbee coordinator channel 26 / PANID 0xd989。
9. **云服务器 API 当前外网访问返回 502**；本次未部署、未登录服务器。需用户明确授权后诊断或执行 `python tools/deploy_server.py --confirm`。
10. 远程开关历史状态：on/off/toggle + on_off 回传已打通；继电器物理声、GPIO5 按键、rejoin 待节点回来复验。
11. **铁律（每次都执行）：会话结束必须更新本地交接文档；上下文压缩会丢记忆，本地化存储才能保留上下文。**
